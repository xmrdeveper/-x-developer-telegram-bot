module.exports = {
  name: 'antinsfw',
  description: 'Filter inappropriate content (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('NSFW filter toggled (placeholder).');
  }
};

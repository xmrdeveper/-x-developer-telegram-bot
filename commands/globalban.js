module.exports = {
  name: 'globalban',
  description: "Placeholder: Add a user to the bot's global ban list.",
  execute: async (bot, ctx) => {
    await ctx.reply('Placeholder: /globalban received. Global ban list updates are disabled.');
  }
};

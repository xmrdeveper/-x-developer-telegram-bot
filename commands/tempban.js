module.exports = {
  name: 'tempban',
  description: "Placeholder: Ban a user for a specified duration (e.g. /tempban @user 1d).",
  execute: async (bot, ctx) => {
    await ctx.reply('Placeholder: /tempban acknowledged. Destructive actions are disabled on this deployment.');
  }
};

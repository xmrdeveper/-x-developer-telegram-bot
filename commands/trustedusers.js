module.exports = {
  name: 'trustedusers',
  description: 'View trusted users (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('Trusted users list is empty (placeholder).');
  }
};

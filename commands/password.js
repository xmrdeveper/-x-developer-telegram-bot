module.exports = {
  name: 'password',
  description: 'Update the admin password (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('Password update requested (placeholder). Do not send passwords in chat.');
  }
};

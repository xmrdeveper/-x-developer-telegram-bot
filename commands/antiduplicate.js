module.exports = {
  name: 'antiduplicate',
  description: 'Prevent duplicate messages (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('Anti-duplicate toggled (placeholder).');
  }
};

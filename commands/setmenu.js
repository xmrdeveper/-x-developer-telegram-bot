module.exports = {
  name: 'setmenu',
  description: 'Change the menu layout.',
  execute: async (bot, ctx) => {
    await ctx.reply('Menu layout change requested (placeholder). Use /settingsmenu to preview.');
  }
};

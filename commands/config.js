module.exports = {
  name: 'config',
  description: 'View current configuration.',
  execute: async (bot, ctx) => {
    await ctx.reply('Current config: (placeholder) — persistent config not implemented.');
  }
};

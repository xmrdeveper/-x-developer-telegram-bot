module.exports = {
  name: 'automation',
  description: 'Open the Automation dashboard.',
  execute: async (bot, ctx) => {
    await ctx.reply('Automation dashboard (placeholder). Use /automenu to view automation commands.');
  }
};

module.exports = {
  name: 'antidocument',
  description: 'Block document sharing (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('Anti-document toggled (placeholder).');
  }
};

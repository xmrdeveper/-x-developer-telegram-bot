module.exports = {
  name: 'vision',
  description: 'Analyze uploaded images.',
  execute: async (bot, ctx) => {
    await ctx.reply('vision: Placeholder. Image analysis requires vision API.');
  }
};

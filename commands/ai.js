module.exports = {
  name: 'ai',
  description: 'Open the AI command center.',
  execute: async (bot, ctx) => {
    await ctx.reply('AI center (placeholder). Use /aimenu to view available AI commands.');
  }
};

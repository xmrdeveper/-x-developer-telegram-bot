module.exports = {
  name: 'autounpin',
  description: 'Automatically unpin expired messages (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('Auto-unpin toggled (placeholder).');
  }
};

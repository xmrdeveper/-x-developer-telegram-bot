module.exports = {
  name: 'autonightmode',
  description: 'Enable scheduled night mode (placeholder).',
  execute: async (bot, ctx) => {
    await ctx.reply('Auto-nightmode toggled (placeholder).');
  }
};

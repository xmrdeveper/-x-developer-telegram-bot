module.exports = {
  name: 'fun',
  description: 'Open the Fun menu.',
  execute: async (bot, ctx) => {
    await ctx.reply('🎲 Fun menu (placeholder). Use /funmenu to view all fun commands.');
  }
};
